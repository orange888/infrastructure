---
apiVersion: v1
kind: Secret
metadata:
  name: traefik-default-cert
  namespace: kube-system
  labels:
    app.kubernetes.io/instance: traefik
    app.kubernetes.io/name: traefik
type: Opaque
data:
  tls.crt: "{{ traefik.tls_crt }}"
  tls.key: "{{ traefik.tls_key }}"
