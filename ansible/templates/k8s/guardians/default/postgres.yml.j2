---
apiVersion: v1
kind: Secret
metadata:
  name: postgres
  namespace: default
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: postgres
data:
  PGPASSWORD: {{ postgres.password | b64encode }}
  PGUSER: {{ postgres.user | b64encode }}
