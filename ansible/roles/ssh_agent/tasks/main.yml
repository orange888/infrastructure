---
- name: Load SSH key into ssh-agent
  shell: ssh-add -t 600 -
  args:
    stdin: |-
      {{ lookup("file", "files/ssh/id_ed25519") }}
  changed_when: false
  delegate_to: localhost
  run_once: true
